NODE_ENV=development
PORT=5001

# MySQL — match DBeaver connection (same host/user/password/database)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=coaching_institute
DB_USER=root
DB_PASSWORD=Gaurav@917

# JWT — use a long random string in production
JWT_SECRET=change_this_to_a_long_random_secret_in_production
JWT_EXPIRES_IN=7d

# Public URL of this API (used in receipt links in dev optional)
API_PUBLIC_URL=http://localhost:5001
