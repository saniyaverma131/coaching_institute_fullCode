// import { Notification } from '../models/index.js';

/** Push in-app notification to a user — disabled (see route call sites). */
export async function notifyUser(_userId, _title, _body = null) {
  // await Notification.create({ user_id: userId, title, body, is_read: false });
}
